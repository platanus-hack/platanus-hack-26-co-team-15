fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
psycopg[binary]>=3.2,<4
sqlalchemy>=2.0,<3
anthropic>=0.68,<1
# servidor MCP (api/app/mcp/): exige Python >=3.10, por eso vive separado
# del pipeline (fijado a 3.9 en el entorno local del lider). Lee de Postgres
# (psycopg, arriba), no de archivos locales. Ver MCP.md.
mcp>=2.0,<3
